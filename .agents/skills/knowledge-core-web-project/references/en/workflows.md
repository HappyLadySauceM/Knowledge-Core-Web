<!-- Generated by Skill Constructor. Edit .skill-constructor/manifest.json through the CLI. -->

# Knowledge-Core-Web: Workflows

## `workflows.development`

Status: `verified`

- **npm:dev**: `{"command":"next dev","source":"package.json"}`
- **npm:start**: `{"command":"next start","source":"package.json"}`

Sources: `detected:project-scripts`, `package.json`, `user-confirmed`, `user-confirmed-development-commands`

Public PR and main verification remain on GitHub-hosted runners. Pushes to dev run the trusted release chain on the organization self-hosted runner label `devops`: build immutable Harbor images with parallelism set to 75% of effective runner CPUs (affinity and cgroup quota), update the source-owned deploy snapshot in GitOps, sync Argo dev, run smoke checks, then promote by fast-forwarding main and releasing. `BUILD_CPU_PERCENT` changes the ratio and `BUILD_JOBS` is only a bounded emergency override. Control-image containers run as the current runner UID and GID (`docker run --user $(id -u):$(id -g)` with a writable HOME); Docker config is mounted at `/ci/docker` rather than `/root/.docker`; runner cache is `$GITHUB_WORKSPACE/../../_cache/knowledge-core`. The host runner uses systemd `HTTP_PROXY=http://127.0.0.1:10991` (sing-box HTTP inbound) with `NO_PROXY` for Harbor, cluster, and npmmirror, and `NODE_USE_ENV_PROXY=1` so Node 24 actions including `create-github-app-token` post honor that proxy; `ci_run` forwards proxy variables into the control image. Self-hosted Node/pnpm/Playwright downloads use npmmirror (`cdn.npmmirror.com` / `registry.npmmirror.com`) and do not restore GitHub Actions package caches. Knowledge-Core-Web/deploy is the editable deployment source and CI synchronizes it automatically to HappyLadySauceM/deploy/Knowledge-Core-Web; deploy-only changes skip image builds while still validating the overlay, updating GitOps, waiting for Argo, running smoke, and promoting main. The self-hosted cleanup is restricted to per-run state and the dedicated Buildx cache. Failures support rollback, and force-push is forbidden.

<!-- fact:cicd.pipeline status:verified sources:.github/workflows/ci.yml, .github/workflows/pipeline.yml, user-confirmed, user-confirmed-cicd-ratio-and-source-deploy-sync, user-confirmed-runner-migration -->

Lint, typecheck, unit tests, production build, e2e, Storybook, Docker build, Kustomize render and server dry-run, Argo sync, and deployment smoke checks pass.

<!-- fact:validation.definition-of-done status:verified sources:user-confirmed -->

## Appendix

## `workflows.build`

Status: `verified`

- **npm:build**: `{"command":"next build","source":"package.json"}`
- **npm:build-storybook**: `{"command":"storybook build","source":"package.json"}`

Sources: `detected:project-scripts`, `package.json`

## `workflows.test`

Status: `verified`

- **npm:test**: `{"command":"vitest run","source":"package.json"}`
- **npm:test:watch**: `{"command":"vitest","source":"package.json"}`

Sources: `detected:project-scripts`, `package.json`

## `workflows.quality`

Status: `verified`

- **npm:lint**: `{"command":"eslint .","source":"package.json"}`
- **npm:typecheck**: `{"command":"tsc --noEmit","source":"package.json"}`

Sources: `detected:project-scripts`, `package.json`

## `cicd.config-files`

Status: `verified`

- `.github/workflows/ci.yml`
- `.github/workflows/pipeline.yml`

Sources: `filesystem:ci-config`
