<!-- Generated by Skill Constructor. Edit .skill-constructor/manifest.json through the CLI. -->

# Knowledge-Core-Web: Environment

### local-dev

- **evidenceCommand**: pnpm dev
- **kind**: pnpm-dev
- **path**: .

### k3s-dev

- **evidenceCommand**: kubectl -n knowledge-core-dev get deploy,svc knowledge-core-web
- **kind**: kubernetes
- **namespace**: knowledge-core-dev
- **path**: deploy/web

### test

- **evidenceCommand**: pnpm lint && pnpm typecheck && pnpm test && pnpm build
- **kind**: github-actions
- **path**: .github/workflows/ci.yml

### production

- **kind**: kubernetes

### canary-gray

- **kind**: kubernetes


<!-- fact:environment.definitions status:verified sources:deploy/web/overlay/dev/config.yaml -->

Use Node.js 24.18.1 (CI pin; package engines allow >=20.9.0) and pnpm 10.31.0. Install with `pnpm install --frozen-lockfile`. Package tarballs use npmmirror via `.npmrc` (`registry=https://registry.npmmirror.com`). ARC CI (`hls-standard` / `hls-builder` ephemeral pods) downloads Node from `https://cdn.npmmirror.com/binaries/node`, skips GitHub Actions pnpm cache restore, and uses the node-local hostPath cache at `/cache` (`NPM_CONFIG_CACHE`, `PLAYWRIGHT_BROWSERS_PATH`, `RUNNER_TOOL_CACHE`). Playwright browsers on ARC CI use `PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright`. Playwright CI `webServer.timeout` is 180s with `PORT=3000` and `HOSTNAME=0.0.0.0`. Inject non-secret `KNOWLEDGE_CORE_GATEWAY_URL` for the BFF; the local default is http://localhost:8080. Cluster overlay deploy/web/overlay/dev points at knowledge-core-gateway in namespace knowledge-core-dev. Secrets stay in GitHub or Kubernetes Secrets. The BFF also accepts KNOWLEDGE_CORE_WEB_ORIGIN for exact Origin checks and KNOWLEDGE_CORE_GATEWAY_TIMEOUT_MS for the outbound timeout budget.

<!-- fact:environment.setup status:verified sources:.github/workflows/pipeline.yml, .npmrc, README.md#section, package.json, user-confirmed, user-confirmed-4x8-hostpath-ubuntu-runtime, user-confirmed-web-bff-session-layer -->

## Appendix

## `environment.toolchains`

Status: `verified`

- `{"manifest":"package.json","toolchain":"node"}`

Sources: `detected:toolchain-manifests`, `package.json`, `user-confirmed`

## `identity.project-name`

Status: `verified`

Knowledge-Core-Web

Sources: `filesystem:project-root`

## `boundaries.repository`

Status: `verified`

- **kind**: `git`
- **nestedRepositories**: `[]`
- **root**: `.`

Sources: `filesystem:repository-boundary`
